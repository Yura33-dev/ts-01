let stringOrNumber: string | number = 2;
stringOrNumber = "somestring";

console.log(stringOrNumber);

let enableOrDisabled: "enabled" | "disabled" = "disabled";

enableOrDisabled = "enabled";

console.log(enableOrDisabled);
